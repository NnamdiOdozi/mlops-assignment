global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: vllm
    static_configs:
      # vLLM runs on the Docker host via scripts/start_vllm.sh.
      - targets: ['host.docker.internal:8000']
    metrics_path: /metrics
